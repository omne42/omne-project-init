[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "__PACKAGE_NAME__"
version = "0.1.0"
description = "__REPO_NAME__"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["__PY_PACKAGE__"]

